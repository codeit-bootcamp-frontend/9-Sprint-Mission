import styled from "styled-components";

const S = {
  Container: styled.div`
    display: flex;
    flex-direction: column;
    gap: 40px;
    width: 1200px;
    margin: 0 auto;
  `,
};

export default S;
